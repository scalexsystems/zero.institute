machine:
  php:
    version: 7.0.4

dependencies:
  cache_directories:
    - "~/.composer/cache/"
  override:
    - composer install --no-progress
    - cp .env.example .env
    - php artisan key:generate
    - php artisan passport:keys

test:
  override:
    - composer test
