machine:
  php:
    version: 7.0.4

dependencies:
  pre:
    - - echo "memory_limit = 1024M" > ~/.phpenv/versions/7.0.4/etc/conf.d/memory.ini
  override:
    - composer install

test:
  override:
    - composer test
